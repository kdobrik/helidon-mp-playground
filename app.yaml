kind: Service
apiVersion: v1
metadata:
  name: helidon-mp-playground
  labels:
    app: helidon-mp-playground
spec:
  type: NodePort
  selector:
    app: helidon-mp-playground
  ports:
  - port: 8080
    targetPort: 8080
    name: http
---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: helidon-mp-playground
spec:
  replicas: 1
  selector:
    matchLabels:
      app: helidon-mp-playground
  template:
    metadata:
      labels:
        app: helidon-mp-playground
        version: v1
    spec:
      containers:
      - name: helidon-mp-playground
        image: helidon-mp-playground
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
